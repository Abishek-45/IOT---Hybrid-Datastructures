class iotDevice {
  constructor(name, type, ) {
      this.data = data;
      this.children = [];
  }
}

class iotNetwork {
  constructor(rootData) {
      this.root = new iotDevice(rootData);
  }
}

